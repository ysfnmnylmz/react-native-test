import React from 'react';
import {Container} from "native-base";
import {Text} from "react-native";

const Sidebar = () => {
  return (
    <Container style={{backgroundColor: 'red', height:200}}>
      <Text>test</Text>
    </Container>
  );
};

export default Sidebar;
